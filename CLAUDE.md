# プロジェクト方針

## スキルの起動ルール

作業に取りかかる前に `.claude/skills/` を確認し、該当するスキルがあれば
**最初の応答・確認質問・ファイル探索よりも先に**起動する。
1%でも該当しそうなら起動する。起動してから「今回は合わない」と判断するのは構わない。

起動したら「〜スキルを使って〜します」と宣言し、スキルの手順どおりに進める。
チェックリストがあるスキルは、項目ごとに TODO を作る。

| 依頼・状況 | 最初に起動するスキル |
|---|---|
| 「〜を作って」「〜を追加して」「〜を変えて」 | `brainstorming` |
| 承認済みの設計から実装計画を作る | `writing-plans` |
| 実装計画に沿って実装する | `executing-plans` |
| 実装コードを書く直前 | `test-driven-development` |
| 「動かない」「テストが落ちる」「CI が赤い」 | `systematic-debugging` |
| レビュー指摘・CI失敗・bot指摘への対応 | `review-triage` |
| 「完了」「直った」と言う直前／コミット・PR の前 | `verification-before-completion` |

**合理化のサイン（これを思ったら止まってスキルを確認する）**

| 思考 | 現実 |
|---|---|
| 「これはただの質問だ」 | 質問もタスク。スキルを確認する |
| 「まず文脈を集めてから」 | スキル確認は確認質問より前 |
| 「まずコードベースを見てから」 | どう見るかをスキルが指示する |
| 「このスキルは大げさ」 | 単純なものはすぐ複雑になる |
| 「内容は覚えている」 | スキルは更新される。現物を読む |

**人間からの明示的な指示は、スキルより優先される。**
スキルの手順を飛ばしてよいのは、人間がそう指示したときだけ。

## 開発の進め方

リサーチ → 計画 → 要件定義 → 設計 → 実装 → フィードバック → 改善、を一気通貫で進める。
実装時は、前提・技術選定の理由・想定される制約やリスクを簡潔に添える。

**実行前に必ず確認を取る操作:**
- データやファイルが消える可能性のあるもの（削除・初期化・上書き）
- 本番環境・課金・外部公開・不可逆な変更を伴うもの
- セキュリティ・権限・認証情報に関わるもの
- 情報として不確実な部分が多いもの

## 情報の扱い

- クラウド（AWS / Azure / GCP）、各種ライブラリの API 仕様・提供ステータス（Preview / GA / 非推奨 / 廃止予定）は、
  記憶に頼らず一次情報で裏取りする
- 出典には取得日・URL・公式／非公式の別を併記する
- 推測・未検証・情報が古い可能性がある内容は、その旨を明示する。**断定と推測を混ぜない**

## レビュー指摘への対応

観点別レビュワー（sub-agent / CI / bot / 人間）から指摘を受けた場合、**コードを編集する前に必ず
`review-triage` スキルを使用する**（`.claude/skills/review-triage/`）。

- 指摘された「問題」は信じる。指摘された「修正方法」は仮説として扱い、鵜呑みにしない。
- Done の定義は「Issue の解決＋リグレッションなし」であり、「レビュワー全員の合格」ではない。
- 同一テーマの指摘が3ラウンド周回したら、実装を止めて人間に判断を仰ぐ。

## このリポジトリの検証コマンド

ビルド不要の静的サイト（HTML / CSS / JS）。テストランナーは無い。
`verification-before-completion` で求められる「証拠」は、次を実行した結果を指す。

```bash
# 1. JS の構文チェック（変更したファイルごと）
node --check path/to/script.js

# 2. Python ツールの構文チェック（tools/ を変更した場合）
python3 -m py_compile tools/build_standalone.py

# 3. ローカルで配信して実機確認（必須）
python3 -m http.server 8000
#   → 変更したページを開き、対象の操作を実行し、
#     DevTools のコンソールにエラー0件であることを確認する
```

**3 を実行していない状態で「動きます」と言わない。**
環境の制約でブラウザ実行ができない場合は、未検証であることと確認手順を明記して報告する。

`test-driven-development` の適用は、SKILL.md の
「テストランナーが無いリポジトリでの適用」に従う（ロジックは純関数に切り出して `node --test`）。

## 構成

- `index.html` — ツール一覧
- `tools/` — ビルド用スクリプト
- `weather/` — 天気ツール
- `assets/` — 共有アセット
- デプロイ: `vercel.json` / `.github/workflows/pages.yml`
