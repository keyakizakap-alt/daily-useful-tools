# つなぎやすみ — 有給の連休プランナーを追加

> とびとびの休みを、つないで見る。

`/yasumi/` に、有給休暇を「いつ・何日」取れば手持ちの日数で一番よく休めるかを計算するアプリを追加する。
既存の2アプリと同じく、依存ライブラリなし・単一 HTML・外部通信なしで動く。

## 1. 解決する課題

有給が残っているのは知っていても、「取り時が分からない」まま年度末に消えていく——という状況を、
祝日・週休・会社の休業日から**計算で**解消する。

企画会議（10名のペルソナによるブレスト、`docs/requirements.md` 1章）で確定した差別化点は3つ。

1. **シフト勤務に対応する** — この手のツールは土日祝休み決め打ちが多く、週休が平日の人が使えない。
   休みの曜日を自由に設定でき、祝日に出勤する働き方にも対応する。
2. **狙い目を効率順に示す** — 「有給2日で9連休」という言い方が、いちばん行動につながる。
3. **完全にローカルで完結する** — 勤務形態や会社の休業日は、勤務先が推測できる情報。一切送信しない。

## 2. 主な機能

- 狙い目ランキング（少ない有給で効きやすい取り方の順に、「できる連休」「今の休み」「取る日」を併記）
- 予算日数内での推奨プラン（厳密な最適化。総当たり探索との一致をテストで検証）
- ねらいの切り替え（連休を増やす / ひとつの長い連休をつくる）
- 会社の休業日の登録（期間＋ラベル、最大20件。年をまたぐ連休も算入）
- 年間カレンダー（クリックで有給をトグル）、`.ics` 書き出し、localStorage への設定保存
- ライト／ダーク、レスポンシブ、キーボード操作対応

## 3. 技術アプローチ

### 3.1 構成

```
src/lib/date.js       UTC 固定・'YYYY-MM-DD' 文字列の日付ユーティリティ
src/lib/holidays.js   国民の祝日 / 振替休日 / 国民の休日（2020〜2050）
src/lib/calendar.js   週休・祝日・会社休業日を重ねた休日タイムライン
src/lib/planner.js    連休の解析・狙い目ランキング・取得日の最適化
src/lib/ics.js        iCalendar (RFC 5545) 出力
src/lib/settings.js   設定の正規化と検証（localStorage は外部入力として扱う）
src/app.js            UI（DOM 生成・イベント）。計算ロジックを持たない
tools/build.mjs       src/ → yasumi/index.html（単一 HTML）を生成
tools/lint.mjs        依存を増やさない静的解析
```

ロジックを ES Modules に分けたのは、**祝日計算と最適化をテスト可能にするため**。
HTML に直書きすると誰も検証できない。一方で「ファイルを開くだけで動く」既存アプリの性質は保ちたいので、
`tools/build.mjs` で単一 HTML に結合し、生成物をコミットする。
ソースとの乖離は `tests/build.test.js` が再ビルド結果とのバイト一致で検出する。

### 3.2 最適化アルゴリズム

タイムラインを「休みの塊」と「出勤日の隙間」の交互列とみなすと、各隙間に対する選択は
`(前から取る日数 a, 後ろから取る日数 b)` の2変数で表せる。`a + b = 隙間の長さ` のとき左右の休みが連結する。
隣接する塊を共有するため隙間どうしは独立ではないので、**「いま開いている連休の長さ」を状態に持つ連鎖 DP** で厳密解を求める。

評価関数は「連休（3日以上）の、3日目以降の日数」の合計。

```
f(n) = max(0, n − 2)         連休を増やす
f(n) = n >= 3 ? n * n : 0    ひとつの長い連休をつくる
```

1年分・予算40日で最悪 60ms（要件は100ms以内）。小さい入力では総当たり探索と最適値が一致することをテストしている。

### 3.3 セキュリティ

- 外部通信・外部フォント・解析をすべて排除し、CSP を `default-src 'none'` に固定。
  `script-src` はビルド時に計算した `'sha256-…'` のみを許可する（`tools/build.mjs`）。
- DOM 生成は `textContent` / `createElement` のみ。`innerHTML` は静的解析で禁止。
- localStorage の値は改竄されうる外部入力として扱い、型・範囲・件数・文字数を検証してから使う。壊れていれば既定値で起動する。
- ユーザー入力（休業日のラベル）は `.ics` 出力時に RFC 5545 のエスケープを通す。改行を混ぜてもイベントが増えないことをテスト済み。
- 実行時依存ゼロ。`npm audit` は 0 件。

## 4. Step 5〜6：レビューと対応

模擬ユーザー3名・UI/UX 2名・セキュリティ2名・技術リードの観点でレビューし、
`.claude/skills/review-triage` の手順で分類してから、根本原因ごとに1つの修正として実装した（全文は `feedback/review_report.md`）。

### 修正した指摘

| 指摘 | 根本原因 | 対応 |
|---|---|---|
| スマホで年間カレンダーの月が重なる | 月グリッドの最小列幅（150px）が、セルの最小寸法（30px×7）を下回っていた | 最小列幅を 232px に統一し、モバイル用の上書きを削除 |
| スマホで横スクロールが出る | `.table-scroll` が `position: static` で、中の絶対配置要素がスクロール領域からはみ出していた | `position: relative` を付与。表見出しの視覚的非表示も廃止 |
| ランキング上位が「GWに1日足して6連休（効率6.0倍）」ばかりで、おすすめと食い違う | ランキングは総量、おすすめは増分という**二重の評価軸**になっていた | ランキングもおすすめと同じ増分評価に統一。「効率○倍」を廃し「今の休み」を併記 |
| 祝日を休みにしないと祝日の表示自体が消える | 「休みか」と「祝日か」を同じフラグで兼ねていた | 祝日は常に保持し、休みにしない設定では文字色だけで示す |
| カレンダーの244個のボタンをキーボードで抜けられない | — | カレンダーの先頭に読み飛ばしリンクを追加（セルの操作性は維持）|
| 設計書に書いたクリックジャッキング対策が未実装 | 実装漏れ | `vercel.json` に `X-Frame-Options: DENY` と `frame-ancestors 'none'` を追加 |

実装中に自己検出した最大の問題は評価関数で、当初の「連休に属する日数の合計」では
**長い休みへの1日追加と、離れた週末を1日でつなぐ取り方が同価値**になり、
「11月と12月の月曜をすべて取る」解が最適と判定されていた。`max(0, n − 2)` に変えてゴールデンウィーク・
シルバーウィークをつなぐ解が出るようになった。

### 却下した指摘（理由つき）

- 「週休を水木にしても土日が休みのまま」→ テストと実機で反証。**誤検知**
- 「UID のフォールバックが衝突しうる」→ 対象ブラウザでは到達しない経路で、UID は秘密値ではない
- 「対象年外の日付が保存に残る」→ 計算・表示とも `selectable` で除外済み。年を往復したときの選択保持を優先
- 「同点解の選び方が走査順依存」→ 仕様上の任意性。受け入れ条件に紐づかないため**スコープ外**

## 5. 検証

| 検査 | 結果 |
|---|---|
| `npm test` | 79件すべてパス |
| `npm run lint` | 指摘なし |
| `npm audit` | 脆弱性 0 件 |
| 祝日の正しさ | 2020〜2026年の公表値と完全一致（振替の連鎖・国民の休日・五輪特例を含む）|
| 最適化の正しさ | 無作為な入力40件で総当たり探索と最適値が一致 |
| 実機（Chromium）| 全操作を通しで実行し、コンソールエラー・CSP 違反ともに 0 件。モバイル 390px で横スクロールなし |

## 6. 先行 PR #20 との重複について（要判断）

作業中に、**未マージの PR #20「やすみつなぎ: 有給の置き方から連休を設計するツールを追加」**
（ブランチ `claude/eloquent-allen-09g5ww`）が同じ着想の実装であることが分かった。
どちらも `docs/requirements.md`・`docs/architecture.md`・`feedback/review_report.md`・`PR_DESCRIPTION.md`・
`package.json`・`.gitignore`・`src/lib/*`・`vercel.json` を**同じパスに追加・変更する**ため、両方はマージできない。
どちらを採るかの判断をお願いしたい（この PR を閉じる形でも問題ない）。

| | PR #20「やすみつなぎ」 | 本 PR「つなぎやすみ」 |
|---|---|---|
| 配信パス | `/src/index.html` をそのまま配信 | `/yasumi/index.html`（既存の `/weather/` と同じ並び）|
| モジュール形式 | UMD（`file://` 対策として classic script）| ES Modules を `tools/build.mjs` で単一 HTML に結合（`file://` でも動く）|
| CSP | 配信ヘッダで `script-src 'self'` | meta で `script-src 'sha256-…'`（ビルド時に算出。単一ファイルのまま固定）|
| 祝日の対応年 | **2007〜2099年**（2019年の即位関連を含む）| 2020〜2050年（五輪特例を含む）|
| 最適化 | 総当たり＋DP（重複しない連休群の合計日数を最大化）| 連鎖 DP による厳密解＋ねらい2種。総当たりとの一致をテストで担保 |
| ランキングの基準 | 有給1日あたりの休日数 | 推奨プランと同じ増分評価（「今の休み」も併記）|
| 会社の休業日 | 休業日＋**特別出勤日**の上書き | 休業日のみ（期間＋ラベル）|

**祝日の対応年は #20 の方が広い**（2007〜2099）。本 PR を採る場合、この範囲拡張は取り込む価値がある。

## 7. 既知の制約

- 春分の日・秋分の日は近似式による算出（1980〜2099年で有効）。
- 国民の祝日は前年2月の官報で確定するため、翌々年以降は現行法にもとづく予測（画面にも明記）。
- 本アプリは労務上の助言ではない。取得可否・付与日数は勤務先の規程に従う。
