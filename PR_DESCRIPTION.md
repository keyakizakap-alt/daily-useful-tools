# やすみつなぎ — 有給休暇の置き方から連休を設計するツール

> その1日で、何日つながる？

## 1. アイデアの概要と解決する課題

有給休暇の話は、たいてい「何日残っているか」で終わる。本当に知りたいのは
**「どこに置けば一番長く休めるか」** なのに、それを教えてくれるものがない。

| 課題 | 現状 | 本ツール |
|---|---|---|
| 有給をどこに置くと得か分からない | カレンダーを指で数える／勘で取る | 期間内の置き方を総当たりで評価し「有給1日あたり何日休めるか」の順に提示 |
| 年5日の取得義務を消化しきれない | 期末に慌てて取る | 予算日数を期間全体に配分し、**重複しない連休群の合計日数を最大化** |
| 飛び石連休に気づけない | 知っている人だけが得をする | 「1日休むと7連休」を自動で発見 |
| 振替休日・国民の休日を読み違える | 祝日一覧を目視 | 祝日法の規定どおりに連鎖計算し、根拠を併記 |
| 会社独自の休業日が計算に入らない | 手計算 | 会社休業日・特別出勤日を日付単位で上書き |

たとえば既定の条件（今日から1年・有給3日・土日休み）で開くと、入力ゼロで
**「2027-04-30(金) に1日休むと 4/29〜5/5 の7連休（有給1日あたり7.0日）」** が先頭に出る。

企画は10体の仮想エージェント（PM / 日本のターゲットユーザー3体 / アーキテクト2体 /
UI・UXデザイナー2体 / セキュリティエンジニア2体）による議論として実施し、
議事録と意思決定の根拠を `docs/requirements.md` に残した。

## 2. 採択した技術アプローチとアーキテクチャ

既存収録アプリ（こいのかたち・そらならべ）の作法に合わせ、**ゼロ依存・ゼロ通信・ゼロビルド**で作った。

```
src/index.html            画面構造・meta CSP・凡例・免責
├─ src/styles.css         テーマ変数（light/dark）、レイアウト、印刷
├─ src/lib/dateutil.js    YYYY-MM-DD の日付演算（タイムゾーン非依存）
├─ src/lib/holidays.js    祝日法に基づく祝日・振替休日・国民の休日の計算
├─ src/lib/calendar.js    週休・会社休業日を合成した「休日カレンダー」の生成
├─ src/lib/planner.js     連休候補の列挙、年間プラン編成（DP）、表示候補の選抜
├─ src/lib/ics.js         RFC 5545 準拠の .ics 生成（エスケープを強制）
└─ src/app.js             DOM 描画・入力・localStorage・ダウンロード
```

### 主要な設計判断

| 判断 | 理由 |
|---|---|
| **祝日は計算で生成**（データファイル・API なし） | 依存ゼロ・通信ゼロを構造で担保。祝日法は完全にアルゴリズム化できる |
| **対応範囲は2007〜2099年** | 振替休日の現行規定（「その後の最も近い祝日でない日」）が2007年施行。範囲外は例外にする |
| **ロジックは UMD 形式**（ES module ではない） | ブラウザは classic script、Node は `require` で**同一コードをテスト**できる。ES module だと `file://` で CORS に阻まれ「ダウンロードして開く」が壊れる |
| **日付は文字列 + UTC 基準の内部表現** | `new Date('2026-09-14')`（UTC 解釈）と `new Date(2026,8,14)`（ローカル解釈）の混在による1日ずれを構造的に排除 |
| **npm 依存 0 個** | サプライチェーン攻撃面を物理的に消す。テストは Node 標準の `node --test` |

### アルゴリズム

**祝日計算** — 3段階で適用する（順序を入れ替えると連鎖判定を誤る）。

1. 本来の祝日（固定日・ハッピーマンデー・春分/秋分の近似式・2019/2020/2021 の特例）
2. 振替休日 — 祝日が日曜のとき、その後の最も近い「祝日でない日」
3. 国民の休日 — 前日・翌日がともに祝日である平日

**連休候補の列挙** — 有給を置く区間の両端を必ず勤務日とし（端が休みの区間は無駄）、
得られた区間を前後の既存の休みで最大まで延長する。O(日数 × 予算) で 366 日でも数ミリ秒。

**年間プラン編成** — 重み付き区間スケジューリング + 予算次元の動的計画法。
`dp[i][b]` を平坦な `Int32Array` で持ち、候補の説明文生成は採用したものだけに遅延させた
（当初 252ms → **8.2ms**）。素朴な全探索との一致をテストで検証している。

### セキュリティ方針

| 脅威 | 対策 |
|---|---|
| 入力（勤務実態）の外部流出 | 通信経路を持たない。CSP `default-src 'none'` / `connect-src 'none'` / `form-action 'none'` |
| 自己 XSS | DOM 書き込みは `textContent` / `createElement` のみ。`make()` が `on*` / `href` / `src` / `style` 属性を受け取ったら例外を投げる。静的解析で `innerHTML` / `eval` を禁止 |
| **ICS インジェクション** | `ics.js` が RFC 5545 のエスケープと制御文字除去を強制。改行での偽 VEVENT / VALARM 注入を単体テストで検証 |
| サプライチェーン | 依存 0 個。CDN 参照なし。ファビコンも埋め込み SVG にして外部リクエストを 0 件にした |
| localStorage の残留（共有PC） | 保存は設定値のみ（開始日も保存しない）。消去ボタンで当アプリのキーを完全に削除 |
| 壊れた保存データ | 読み出し時に型・範囲・件数上限を検証し、不正なら既定値へ安全に復帰 |
| 巨大入力による DoS | 日付リストは500件、期間は366日で上限 |

## 3. Step 5〜6 で行った変更点とフィードバック対応

模擬ユーザー3体・セキュリティ2体・アーキテクト2体の観点でレビューし、**14件**の指摘を得た。
リポジトリの方針（`CLAUDE.md`）に従い、コードを編集する前に `review-triage` スキルを通して
分類・統合した。全記録は `feedback/review_report.md`。

14件の指摘は **4つの根本原因** に収束した（指摘の数だけパッチを当てていない）。

### 根本原因1 — 候補の提示に「時期の多様性」がない

おすすめプラン12件が、ゴールデンウィークとシルバーウィークの変種で埋まり、実質4時期しか
含まれていなかった。効率という一次元だけで並べたため、「同じ連休の変種」と「別の時期の候補」が
同じ土俵で競合していた。

`planner` に純関数を2つ追加し、表示ロジックを `app.js` から移して単体テストの対象にした。

- `dropDominatedPlans()` — 上位候補に包含され、かつ有給も同じか多い劣った候補を落とす
- `selectVariedPlans()` — 範囲が重なる候補を最大2件までに抑えて選抜する

初心者（選択肢を減らしたい）とヘビーユーザー（「1日で7連休」と「2日で9連休」を見比べたい）の
要求が衝突していたため、**同じ時期は2件まで残す**ことで両立させた。結果、12件が**9時期**に分散した。

### 根本原因2 — テーマ状態の単一情報源が localStorage になっていた

この設計が、独立に見える2つのバグを生んでいた。

- 「保存した条件を消す」を押しても `yasumitsunagi.theme.v1` が残る
  （`removeItem` した直後に `applyTheme()` が書き戻していた）
- localStorage が例外を投げる環境（プライベートモード等）で、テーマ切替が「ライト」で固着する
  （クリックごとに保存値を読み直すため、保存が失敗すると次の状態を計算できない）

`state.theme` を単一の情報源にし、永続化を副作用として分離（`applyTheme(theme, { persist })`）。
消去時は `persist: false` で書き戻さないようにした。両方のバグにブラウザ確認の項目を追加している。

### 根本原因3 — 安全性が人間の注意力に依存していた

CSP の不要な緩和、meta では無視される指令、`make()` の無制限な属性設定を、いずれも
「レビューで」発見した。守るべき不変条件がコードにも静的解析にも書かれていなかったのが原因。

- `style-src` から `'unsafe-inline'` を削除（`element.style` 経由の CSSOM 操作は CSP の管轄外で不要）
- `frame-ancestors` は meta では無視されるため削除し、`vercel.json` の HTTP ヘッダへ移動
  （既存2アプリを壊さないよう `/src/(.*)` にスコープを限定）
- `make()` に属性ガードを追加し、静的解析に CSP 2ルールと lockfile 検査を追加

### 根本原因4 — 祝日計算の前提が暗黙だった

年境界（12/31 ↔ 1/1）をまたぐ振替休日・国民の休日を扱えない。現行法では発生しないが、
その前提がコードにもテストにも書かれていなかった。

前後年を参照する実装には**しなかった**（複雑さに対して得るものがない）。代わりに前提を
コメントで明文化し、**前提が崩れたら落ちるテスト**を置いた（2007〜2099年の全域で
12/30・12/31・1/2 に祝日がないこと）。

### 意図的に変更しなかった指摘

| 指摘 | 判断 |
|---|---|
| `enumerateCandidates()` の二重実行をキャッシュすべき | **却下（誤検知）**。実測 1.5ms × 2 で体感差がなく、キャッシュは「入力が変わったのに古い候補を返す」不整合の余地を作る |
| 半休（0.5日）に対応すべき | **スコープ外**。`docs/requirements.md` の非ゴール。探索空間が2倍になる一方、提案の質は上がらない。別Issueとする |
| 有給の残日数・繰越も管理すべき | **スコープ外**。勤怠管理は別プロダクトの領域。別Issueとする |

### その他の対応

- `renderSummary()` が `parentNode.querySelector('h2')` で見出しを辿っていたのを id 取得に変更
- `.ics` 書き出しの結果メッセージを、フッター隅から押したボタンの隣へ移動
- まとめの「期間」が見出しと重複していたので「有給なしの連休の最長日数」に差し替え

## 4. 検証結果

| 検証 | 結果 |
|---|---|
| `npm test`（Node 標準 `node --test`） | **114件 / 全パス** |
| `npm run test:tz`（UTC / Asia/Tokyo / America/Los_Angeles） | 3環境すべて114件パス（日付のずれなし） |
| `npm run check`（自作静的解析） | 指摘0件 |
| `npm audit` | **脆弱性0件**（依存0個） |
| `npm run verify:browser`（Chromium 141、`file://` と `http://`） | **49項目すべてOK** |

### テストの内訳

| ファイル | 主な検証内容 |
|---|---|
| `tests/dateutil.test.js` | 閏年、月末・年末越え、タイムゾーン非依存、不正入力 |
| `tests/holidays.test.js` | 2026年の全祝日の完全一致、既知の振替休日（2021-08-09 / 2024年の4回 / 2007年）、国民の休日（2019-04-30・05-02 / 2026-09-22）、2019・2020・2021年の特例、春分/秋分の確定日15年分、年単位計算の前提 |
| `tests/calendar.test.js` | 週休の任意指定、会社休業日、特別出勤日の優先順位、上限・不正入力 |
| `tests/planner.test.js` | 効率計算、飛び石の検出、順序の決定性、年間プランの非重複性・予算遵守・**全探索との一致**、候補選抜 |
| `tests/ics.test.js` | RFC 5545 エスケープ、**CRLF 注入・VALARM 注入**、75オクテット折返し（マルチバイト・サロゲートペア）、UID の決定性 |
| `tests/integration.test.js` | 実際の暦での一連の整合（2026年シルバーウィーク・GW・夏季休業）、性能（366日 < 100ms） |

### ブラウザ確認が実際に見つけた問題

自動確認が、コードレビューでは見落としていた2件を検出した。

- `frame-ancestors` を meta で宣言しても無視されるという警告 → HTTP ヘッダへ移動
- ファビコン未指定による 404 → 埋め込み SVG ファビコンで外部リクエストを0件に

### 計算結果の目視確認

スクリーンショット（`verify-output/`、`.gitignore` 済み）で実際の暦との一致を確認した。

- 2026-09-21 敬老の日 / 09-22 **国民の休日** / 09-23 秋分の日 → 9/19(土)から5連休
- 2027-03-21 春分の日(日) → 03-22 **振替休日**
- 2026-05-03 憲法記念日(日) → 5/4・5/5 が祝日のため **5/6 が振替休日**
- 2027-04-29 昭和の日 + 04-30 に有給1日 → 7連休（首位）

## 5. 成果物

```
docs/requirements.md        要件定義書（10エージェントの議事録・ユースケース・受け入れ条件）
docs/architecture.md        設計・システム構成・アルゴリズム・脅威モデル・セキュリティ方針
src/                        MVP 実装（index.html / app.js / styles.css / lib/*.js）
tests/                      単体・結合テスト 6ファイル / 114件
feedback/review_report.md   レビュー報告書（トリアージ表・根本原因・却下理由・再検証結果）
tools/static-check.js       自作の静的解析
tools/verify-browser.js     Chromium での実機確認（49項目）
PR_DESCRIPTION.md           本ファイル
.gitignore                  .env / node_modules / ビルド成果物 / 確認出力
package.json                依存0個。test / test:tz / check / verify:browser / audit
package-lock.json           npm audit を成立させるための、依存0個の lockfile
```

## 6. 既知の制約

- 春分の日・秋分の日は近似式による計算値（法的には前年2月の官報告示で確定）。将来の法改正も反映しない。
  画面・README・要件定義のいずれにも明示している。
- 対応範囲は2007年〜2099年。
- 半休・時間単位年休、有給の付与日数・時効・繰越の管理、不規則シフトには非対応（いずれも非ゴール）。
- 配信パスが `src/` のままで見栄えがよくない。本タスクの指示（実装は `src/` 配下）を満たすことを
  優先し、生成物の二重管理を避けた。リリース時は `vercel.json` の rewrite か `yasumi/` への移設で対応する。
